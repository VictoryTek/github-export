{
  "name": "github-export",
  "version": "0.1.0",
  "private": true,
  "description": "Desktop app for managing GitHub issues, PRs, and security alerts",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "dev:mock": "tauri dev --features dev-mock",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1"
  }
}
